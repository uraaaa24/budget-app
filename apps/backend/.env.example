# This file is kept for reference but is NO LONGER USED in Cloudflare Workers setup
# For local development with wrangler, use .dev.vars instead (see .dev.vars.example)

# Production environment variables should be set in Cloudflare Dashboard:
# Dashboard → Workers & Pages → Your Worker → Settings → Variables and Secrets

API_URL=http://localhost:8787
CLERK_SECRET_KEY=
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres
